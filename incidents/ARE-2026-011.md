# ARE-2026-011: Installation of a hallucinated or look-alike dependency (react-codeshift slopsquat)

> `ARE-2026-011` &nbsp;·&nbsp; **OWASP ASI:** ASI04 Supply Chain &nbsp;·&nbsp; **Coverage class:** Action-coverable &nbsp;·&nbsp; **Severity:** 1

**Failure mode:** `SUPPLY_CHAIN` &nbsp;·&nbsp; **Confusion vector:** `HALLUCINATED_RESOURCE` &nbsp;·&nbsp; **Surface:** package-install

**Coverage class:** Action-coverable -- an inspectable tool call, addressable by deterministic action interception before it runs.

## What happened

An agent installed a package that rode a famous name, a hallucinated or look-alike dependency rather than the real one.

## Blast radius

Malicious code pulled into the build via a typo-squatted or freshly published package.

## Source

<https://medium.com/@instatunnel/ai-hallucination-squatting-the-new-agentic-attack-vector-6e1366d77038>

---

## Vendor coverage claims

_Claims by vendors about their own products, not registry findings. The registry records what was claimed, by whom, and whether the check still passes; it does not rank or endorse vendors. Any vendor may add a claim under its own prefix; see [CONTRIBUTING.md](../CONTRIBUTING.md)._

**AgentX Core** (the registry maintainer) claims: **Covered -- blocked deterministically today**, wired to the AgentX gateway. The full claim, including what it does not stop, is at [agentx-core.com/aredb](https://agentx-core.com/aredb).

The gateway routes an install of a package that the public registry reports as not found, freshly published, or a look-alike of a popular name to a human for approval, unless a trust signal (registry membership, provenance, same publisher) rescues it. An escalation, not a hard block, since a not-found is often a legit private package.

**Repro (gateway).** This block runs in the AgentX gateway, so it does not fire from a bare `pip install`. The gateway is free and self-serve: pull it at [agentx-core.com/gateway](https://agentx-core.com/gateway) and run it locally to reproduce this claim.
