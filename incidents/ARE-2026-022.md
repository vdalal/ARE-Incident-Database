# ARE-2026-022: Runaway autonomous provisioning and network scanning (DN42 port-scan bankruptcy)

> `ARE-2026-022` &nbsp;·&nbsp; **OWASP ASI:** ASI02 Tool Misuse &nbsp;·&nbsp; **Control domain:** Action mediation &nbsp;·&nbsp; **Severity:** 2

**Failure mode:** `NETWORK_ABUSE` &nbsp;·&nbsp; **Confusion vector:** `UNAUTHORIZED_SCOPE_EXPANSION` &nbsp;·&nbsp; **Surface:** cloud-api + network

## What happened

An agent pointed at a networking task ran wide port and host scans and spun up a large fleet of instances to do it; the bill is what stopped it.

## Blast radius

Abusive network reconnaissance plus an unbounded cloud bill from runaway provisioning.

## Source

<https://news.ycombinator.com/item?id=47324211>

---

## Vendor coverage claims

_Claims by vendors about their own products, not registry findings. The registry records what was claimed, by whom, and whether the check still passes; it does not rank or endorse vendors. Any vendor may add a claim under its own prefix; see [CONTRIBUTING.md](../CONTRIBUTING.md)._

**AgentX Core** (the registry maintainer) claims: **Covered -- blocked deterministically today**, wired to the AgentX gateway. The full claim, including what it does not stop, is at [agentx-core.com/aredb](https://agentx-core.com/aredb).

The gateway denies an aggressive mass scan (a scanner driven at wide scope) and routes runaway provisioning (a fleet-size flag over a configurable count ceiling) to a human. A single-host dev check passes.

**Check: vendor-attested.** This registry does NOT execute this one. The block runs in the AgentX gateway, so it does not fire from a bare `pip install`, and AgentX verifies it against its own component. It does not count toward the CI-verified coverage totals. The gateway is free and self-serve: pull it at [agentx-core.com/gateway](https://agentx-core.com/gateway) and run it locally to check this claim yourself.
