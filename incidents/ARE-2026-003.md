# ARE-2026-003: Recursive whole-scope deletion in the wrong environment (25k-document wipe)

> `ARE-2026-003` &nbsp;·&nbsp; **OWASP ASI:** ASI02 Tool Misuse &nbsp;·&nbsp; **Control domain:** Action mediation &nbsp;·&nbsp; **Severity:** 1

**Failure mode:** `DESTRUCTIVE_ACTION` &nbsp;·&nbsp; **Confusion vector:** `ENVIRONMENT_CONFUSION` &nbsp;·&nbsp; **Surface:** filesystem

## What happened

An agent ran the right command in the wrong environment and deleted about 25,000 documents.

## Blast radius

Recursive, whole-scope deletion of a document store. Correct command, wrong target.

## Source

<https://www.reddit.com/r/ClaudeAI/comments/1rshuz9/an_ai_agent_deleted_25000_documents_from_the/>

---

## Vendor coverage claims

_Claims by vendors about their own products, not registry findings. The registry records what was claimed, by whom, and whether the check still passes; it does not rank or endorse vendors. Any vendor may add a claim under its own prefix; see [CONTRIBUTING.md](../CONTRIBUTING.md)._

**AgentX Core** (the registry maintainer) claims: **Covered -- blocked deterministically today**, from the keyless SDK (no key, no gateway). The full claim, including what it does not stop, is at [agentx-core.com/aredb](https://agentx-core.com/aredb).

The keyless shield denies recursive whole-scope deletion (a root or home path wiped by shell or by a structured delete-with-recursive) before it runs. The narrower "right command, wrong environment" judgment is left to the org's ground truth, not faked.

**Check: CI-verified.** This registry runs this snippet on every push. It blocks from a bare `pip install`, no key, no gateway; the decision is made on your machine. Two notes: the SDK only watches by default, so `posture="enforce"` is what makes it block; and it sends usage telemetry at exit unless you set `AGENTX_TELEMETRY=off`. Copy it and run it.

```bash
pip install agentx-security-sdk
```

```python
from agentx_sdk import agentx_protect, is_block

@agentx_protect(agent_id="aredb-repro", action="db_write", posture="enforce")
def run_sql(query: str):
    return "EXECUTED"          # the agent never gets here

result = run_sql("DELETE FROM documents")
print(is_block(result))        # True
print(result)                  # the block, and the safe path to take instead
```

---

_AREDB is not an OWASP project and is not affiliated with or endorsed by OWASP. It indexes onto the ASI Top 10 because that is the vocabulary the field is standardizing on. See [`RELATION-TO-STANDARDS.md`](../RELATION-TO-STANDARDS.md)._
