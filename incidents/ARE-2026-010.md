# ARE-2026-010: Unapproved high-value fund transfer (Lobstar Wilde $450k loss)

> `ARE-2026-010` &nbsp;·&nbsp; **OWASP ASI:** ASI02 Tool Misuse &nbsp;·&nbsp; **Control domain:** Action mediation &nbsp;·&nbsp; **Severity:** 1

**Failure mode:** `FINANCIAL` &nbsp;·&nbsp; **Confusion vector:** `GOAL_COMPLETION_BLINDNESS` &nbsp;·&nbsp; **Surface:** transaction

## What happened

An agent moved roughly $450,000 in a value transfer that should have paused for a human.

## Blast radius

Irreversible high-value fund movement. Role-based access authorizes the agent, so only a consequence-aware approval gate catches it.

## Source

<https://www.reddit.com/r/CryptoCurrency/comments/1rfvnb5/the_lobstar_wilde_450k_loss_wasnt_a_decimal_error/>

---

## Vendor coverage claims

_Claims by vendors about their own products, not registry findings. The registry records what was claimed, by whom, and whether the check still passes; it does not rank or endorse vendors. Any vendor may add a claim under its own prefix; see [CONTRIBUTING.md](../CONTRIBUTING.md)._

**AgentX Core** (the registry maintainer) claims: **Covered -- blocked deterministically today**, wired to the AgentX gateway. The full claim, including what it does not stop, is at [agentx-core.com/aredb](https://agentx-core.com/aredb).

The gateway routes a high-value transfer (a value-movement verb plus a monetary signal over a configurable threshold) to a human for approval before it executes. Small or non-monetary transfers pass.

**Check: vendor-attested.** This registry does NOT execute this one. The block runs in the AgentX gateway, so it does not fire from a bare `pip install`, and AgentX verifies it against its own component, so it is not included in the registry's CI-verified count. The gateway is free and self-serve: pull it at [agentx-core.com/gateway](https://agentx-core.com/gateway) and run it locally to check this claim yourself.
